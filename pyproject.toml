[project]
name = "tennis-simulator"
version = "1.0.0"
description = "Monte Carlo tennis match simulator using real player statistics"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.1,<4",
    "gunicorn>=23,<24",
]
